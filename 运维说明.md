# serhui-proxy 运维说明

Vercel 上的 Google API 出海通道。项目名 `serhui-proxy`，域名 `srm-x.serhui.com`，锁定区域 `sin1`（新加坡）。

## 一、两个独立入口

| 入口 | 实现 | 用途 | 认证 |
| :--- | :--- | :--- | :--- |
| `/api/ws` | `api/ws.js` | VLESS-over-WebSocket 隧道（裸 TCP 转发，可连任意主机） | VLESS UUID |
| `/(.*)` | `api/proxy.js` | HTTP 反代（按路径改写，仅 `*.googleapis.com`） | `PROXY_TOKEN` |

**关键区别：**

- **隧道**能转发任意 TCP，抗探测靠 UUID；`new-api` 走的就是这个（mihomo 节点 `🟢 Vercel-SG` → 本地 7892）。
- **反代**只能转发 googleapis，靠路径改写工作，**无法用作渠道 proxy**（渠道需要正向代理的 `CONNECT`，Vercel 平台层直接拒绝）。

## 二、抗探测设计（重要）

未授权请求**一律返回相同的通用 404**，不暴露任何技术细节。

```bash
# 四个入口的匿名响应完全一致（同一 md5）
curl https://srm-x.serhui.com/api/ws               # 404 通用 HTML
curl https://srm-x.serhui.com/                     # 404 通用 HTML
curl https://srm-x.serhui.com/__diag               # 404 通用 HTML
curl https://srm-x.serhui.com/xxx.googleapis.com/  # 404 通用 HTML
```

**历史教训**：早期版本 `/api/ws` 在非 WebSocket 请求下返回 `400 vless ws tunnel only`，
等于用一条普通 HTTPS 请求自报家门 —— 任何扫到该域名的探测者都能据此确认这是 VLESS 隧道。
现已改为通用 404。

**约束**：新增任何入口都必须遵守此约定 —— 不要返回 `401`/`403` 或带技术字样的响应体。
`401` 本身就暴露了"此处存在受保护资源"。

## 三、访问方式

### 反代（需 token）

```bash
TOKEN=$(cat /root/bin/serhui-proxy/.proxy_token)
curl -H "Authorization: Bearer $TOKEN" \
  https://srm-x.serhui.com/www.googleapis.com/discovery/v1/apis
```

也可用 `?__token=<token>` 查询参数（便于浏览器直接访问）。

`PROXY_TOKEN` 存于 Vercel 项目环境变量（encrypted，三个 target 各一份）。
**留空则关闭校验**（回退到无认证模式）。

### 隧道

由 mihomo 使用，配置在 `/etc/mihomo/config.yaml`：

```yaml
- name: "🟢 Vercel-SG"
  type: vless
  server: srm-x.serhui.com
  port: 443
  uuid: 7ec6d45a-9ef0-4418-8c92-f076448e9c09
  network: ws
  tls: true
  servername: srm-x.serhui.com
  ws-opts:
    path: /api/ws
    headers:
      Host: srm-x.serhui.com
```

入站 `127.0.0.1:7892`（仅监听本机），供 new-api 渠道 3 使用。
UUID 可用环境变量 `TUNNEL_UUID` 覆盖。

## 四、性能实测

| 路径 | 吞吐 | TTFB | 说明 |
| :--- | ---: | ---: | :--- |
| 反代（直连 Vercel） | 620 KB/s | 0.38s | 最快，但**渠道用不了** |
| 隧道（7892） | 380 KB/s | 0.79s | 当前方案，隧道封装开销约 38% |
| 香港中转（7891） | 91 KB/s | — | 已被线路限速，作备用 |

端到端（经隧道）：TTFT 中位 2.88s，吞吐 171.7 tok/s。

**隧道 38% 开销的来源**：VLESS 帧头 + WebSocket 分帧 + 双重 TLS 处理 + Vercel 用户态转发。
即便如此仍比香港中转快 4 倍 —— 后者是线路本身被限速。

## 五、区域选择

Vercel 亚洲区域实测（同域名单项目逐一部署对比，各 10 次）：

| 区域 | 位置 | 下载中位 | 结论 |
| :--- | :--- | ---: | :--- |
| `sin1` | 新加坡 | 528 KB/s | ✅ 最优（离 Google 亚太最近） |
| `hnd1` | 东京 | 500 KB/s | 接近 |
| `icn1` | 首尔 | ~490 KB/s | 接近 |
| `kix1` | 大阪 | ~330 KB/s | 偏慢 |
| `hkg1` | 香港 | — | TCP 218ms，最差（走 AWS ap-east-1，与本地香港服务器不是同一线路） |

**台湾无节点**（`tpe1` 被拒：`Invalid region selector`）。

Vercel 有 126 个 PoP 负责就近接入，流量经其内网到区域机房 —— 所以各区域差异被压缩在 10% 以内。
**真正的瓶颈始终是"本机→最近 PoP"这一段**，由运营商国际出口决定，换区域改不了。

## 六、部署

```bash
cd /root/bin/serhui-proxy
export VERCEL_TOKEN='<token>'
vercel deploy --prod --yes --token "$VERCEL_TOKEN" --scope perry21
```

修改 `vercel.json` 的 `regions` 即可切换区域。注意 `maxDuration` 必须放在 `functions` 段内
（顶层的写法在新版已不被接受）。

**旧项目 `vercel-proxy` 保持不动**，作为另一条备用通道。

## 七、已知风险

1. **Vercel 免费额度 100GB/月** —— 当前用量几十 MB/天。别把域名分享出去当公共代理。
2. **`PROXY_TOKEN` 泄露不会影响隧道** —— 两者独立，反代失守不影响 new-api 的隧道通路。
3. **隧道 UUID 泄露 = 完整翻墙出口** —— 与反代相反，这是隧道模式的固有风险。
