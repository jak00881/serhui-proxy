{
  "name": "serhui-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Google API reverse proxy + VLESS-over-WebSocket tunnel on Vercel (sin1)",
  "dependencies": {
    "ws": "^8.18.0"
  }
}
