{
  "name": "serhui-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "VLESS-over-WebSocket tunnel + Google API reverse proxy, deployable on Render (singapore)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
